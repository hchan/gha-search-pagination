
name: zgIvJpWxdx Workflow
description: This is a simple workflow vGCkeUTdatNHtCgvOFrs
on:
  push:
    branches:
      - main
    paths: 
      - .github/workflows/workflow2.yml
  workflow_dispatch:

jobs:
  hello_world_job:
    runs-on: ubuntu-latest

    steps:
    - name: Say Hello
      run: echo "From workflow2.yml" >> $GITHUB_STEP_SUMMARY
