
name: XZRljxolBL Workflow
description: This is a simple workflow Ozh56ejsszBzsLpqkM3G
on:
  push:
    branches:
      - main
    paths: 
      - .github/workflows/workflow10.yml
  workflow_dispatch:

jobs:
  hello_world_job:
    runs-on: ubuntu-latest

    steps:
    - name: Say Hello
      run: echo "From workflow10.yml" >> $GITHUB_STEP_SUMMARY
