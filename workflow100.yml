
name: j5pQvvpFbh Workflow
description: This is a simple workflow fsQHW4bvCjGnwyKVZFvx
on:
  push:
    branches:
      - main
    paths: 
      - .github/workflows/workflow100.yml
  workflow_dispatch:

jobs:
  hello_world_job:
    runs-on: ubuntu-latest

    steps:
    - name: Say Hello
      run: echo "From workflow100.yml" >> $GITHUB_STEP_SUMMARY
