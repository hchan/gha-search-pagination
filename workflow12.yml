
name: 4P1AKMWTWj Workflow
description: This is a simple workflow ppfXy1llye8I5XMo9uP6
on:
  push:
    branches:
      - main
    paths: 
      - .github/workflows/workflow12.yml
  workflow_dispatch:

jobs:
  hello_world_job:
    runs-on: ubuntu-latest

    steps:
    - name: Say Hello
      run: echo "From workflow12.yml" >> $GITHUB_STEP_SUMMARY
