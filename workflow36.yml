
name: 920PUtvfIq Workflow
description: This is a simple workflow eEEVtUXzql8jdq187x0I
on:
  push:
    branches:
      - main
    paths: 
      - .github/workflows/workflow36.yml
  workflow_dispatch:

jobs:
  hello_world_job:
    runs-on: ubuntu-latest

    steps:
    - name: Say Hello
      run: echo "From workflow36.yml" >> $GITHUB_STEP_SUMMARY
